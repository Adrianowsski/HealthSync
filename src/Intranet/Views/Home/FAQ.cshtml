@{
    ViewData["Title"] = "FAQ";
}

<h2 class="mb-4"><i class="bi bi-question-circle-fill me-1"></i> Frequently Asked Questions</h2>

<div class="bg-white shadow-sm p-4 rounded">
    @Html.Raw(ViewData["Content"])
</div>